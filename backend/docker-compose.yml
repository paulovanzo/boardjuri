version: "3"

services:
  db:
    image: mysql:5.7
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: dbuser
      MYSQL_ROOT_PASSWORD: DBUsers12*
      MYSQL_USER: root
      MYSQL_PASSWORD: DBUsers12*
  backend:
    depends_on:
      - db
    build: .
    restart: unless-stopped
    command: npm run dev
    volumes:
      - ./:/usr/app/
    ports:
      - "3333:3333"
